<IfModule mod_rewrite.c>

# Enable Rewrite Engine
RewriteEngine On
RewriteBase /

# Redirect Urls (Permanent)
RewriteRule (.*) https://meetupofmeetup.commeetuporganisers.com/$1 [R=302,L]

</IfModule>
